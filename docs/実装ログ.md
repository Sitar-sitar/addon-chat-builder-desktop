# 実装ログ（addon-chat-builder-desktop）

`D:\my-app2\docs\実装フェーズ記録ルール.md` に従い、実装の過程をフェーズ単位で記録する。
次フェーズ着手前に現フェーズを「締めチェックリスト」で必ずクローズする。

- 対象アプリ: addon-chat-builder-desktop（`apps/web`）
- 現在フェーズ: Java版対応 v0.2.0
- 現在ステータス: 実装完了・Java実ゲーム受入待ち

---

## 現在進行中フェーズ

### Phase 1: Java版対応 v0.2.0

- 目的 / スコープ: Bedrock既存機能を維持し、Java 1.21系向けのレシピ・定期チャット通知・langリソースパックを決定論的に生成する。
- 対象バージョン: apps/web v0.2.0
- 関連設計書: [機能設計書_Java版対応_2026-07-18.md](機能設計書_Java版対応_2026-07-18.md)
- 関連レビュー: [レビュー_Java版対応設計_2026-07-18.md](レビュー_Java版対応設計_2026-07-18.md)
- 着手日: 2026-07-18
- 作業ブランチ: `feature/java-edition-support`
- ステータス: 実装完了・受入待ち（未クローズ）

**作業メモ（随時追記）**

- 2026-07-18: レビュー全9件反映済みの設計正本（コミット `767e02f`）を再確認して着手。
- 2026-07-18: このPCのMinecraft Java実機は `1.21.5`（加えて `1.21.6-pre2`）を確認。受け入れ確認用に `JAVA_VERSIONS` へ1.21.5を追加し、ローカル `.env` で選択する方針。既定1.21.7は維持。
- 2026-07-18: `AddonSpec` のedition/kind契約、Java用バージョン表・決定論的生成器、版別ZIP名、Java専用出力先を実装。JavaのCodex生成は行わず、既存Bedrock経路だけCodexを維持。
- 2026-07-18: UIへ統合版/Java版切替、Java 1.21.5表示、版別開始例、処理中切替禁止、世代idによる遅延応答破棄、版別生成ラベルを実装。Chromeで設定表示、処理中disable、処理後復帰、生成完了表示、切替確認ダイアログを確認。
- 2026-07-18: Java 1.21.5向けにレシピ・定期通知・langのZIP 3種を本番API経由で `D:\my-app2\Minecraft_Addon\javaパックファイル` へ生成。pack formatはデータパック71、リソースパック55、内部パスとJSONを確認。
- 2026-07-18: 検証結果——`npm run lint` 成功、Vitest 6ファイル/24件成功、`npm run build` 成功、`dotnet build AddonChatBuilder.Desktop.sln --configuration Release` 成功（警告0/エラー0）、設計書検証成功。Next.jsビルドには既存の動的`.env`読込に関するNFTトレース警告1件が残るが、ビルド成果物と全ルート生成は成功。
- 2026-07-18: WPF/WebView2側 `src/` のコード変更は0件。ローカル `.env` に `DEFAULT_OUTPUT_DIR_JAVA` と `JAVA_TARGET_VERSION=1.21.5` を追加（Git管理外）。
- 2026-07-18: 実装コミット `786e280`（`feat: Minecraft Java版パック生成に対応`）へ反映。実ゲーム受入待ちのためフェーズは未クローズ、master未マージ。

**受入待ちゲート**

- 設計書 §8 に従い、Minecraft Java 1.21.5実ゲームで次の3件を確認するまで本フェーズはクローズせず、masterへマージしない。
  1. `java-recipe-1215-datapack.zip`: 作業台でダイヤモンド1個から棒2本をクラフトできる。
  2. `java-notify-1215-datapack.zip`: `Java版対応の実機確認` が5秒間隔で表示され、`/reload` 後も二重予約しない。
  3. `java-lang-1215-resourcepack.zip`: ダイヤモンドの剣が `Java版確認の剣` と表示される。
- 現在の `.minecraft` に既存ワールドがなく、CodexからMinecraftクライアントを対話操作できないため、実ゲーム確認だけをユーザー実施待ちとする。自動検証・生成物作成・UI/API確認は完了済み。

---

## 完了フェーズ（締め済み）

なし。
