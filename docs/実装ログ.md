# 実装ログ（addon-chat-builder-desktop）

`D:\my-app2\docs\実装フェーズ記録ルール.md` に従い、実装の過程をフェーズ単位で記録する。
次フェーズ着手前に現フェーズを「締めチェックリスト」で必ずクローズする。

- 対象アプリ: addon-chat-builder-desktop（`apps/web`）
- 現在フェーズ: なし（Java時刻/天候アクションの設定化 v0.7.3 実装完了・本番差し替え済み）
- 現在ステータス: 完了（自動検証は全 pass・本番反映済み。event trigger と 26.2 の実ゲーム受入のみ別スコープ待ち）

---

## 現在進行中フェーズ

なし。

---

## 完了フェーズ（締め済み）

### Phase 5: Java時刻/天候アクションの設定化 v0.7.3 — 完了

- 完了日: 2026-07-24
- 実装した内容: setTime/setWeather の意味づけを one-shot の「設定」へ変更し、v0.7.2 の interval 専用制限を撤回。M1: `validateJavaScript` の interval 専用チェックを削除し event 5種でも使用可に。M2: 確定文言へ統一（ラベル「時刻固定/天候固定」→「時刻設定/天候設定」、promptLine、検証エラー「固定する〜」→「設定する〜」、`describeAction`）。レシピの「完成数は1固定」と message/title/actionbar の「固定文」は非対象として維持。M3: 生成 README へ one-shot 注記（setTime または setWeather 時）と dimension 注記（setTime 時のみ）を条件付き追加。M4: `apps/web/README.md` の「定期実行トリガーでのみ使えます」を解消し、ルート `README.md` と併せて v0.7.3 を後続正本として追記。生成コマンド（`time set` / `weather`）は不変。
- 反映コミット: `8e4aba5`（実装本体：src/tests/README/version）／文書同期は後続の docs コミット
- 検証結果: `npm run lint`（tsc）pass / Vitest 8ファイル51件 pass（45→51。event5種×2アクション=10ケース許可、確定文言の完全一致 assertion 新設、README 注記の3分岐、v0.7.2 差分 whitelist を追加）/ `npm run build` pass / WPF Release build pass（警告0・エラー0）。**本番差し替え済み**: self-contained publish 実行（`publish\win-x64\AddonChatBuilder.Desktop.exe` SHA-256 `14ac5ebe7e0b104498d7d32282841b9d38b6e3be835e8045ee994fc505df6437`）、Next 本番ビルドID `-6LNg0mKEm-bThdmZLmHP`、本番チャンクに新文言「時刻設定」を確認、旧文言「時刻固定」は0件。**実ゲーム受入：未実施**。
- 残課題・既知の問題: 設計書 §8 の手動・実ゲーム項目が未実施。(a) event trigger（consumeItem + setTime）を 1.21.5 で導入し1回だけ設定されることの確認、(b) scoreboard 系 event（mineBlock + setWeather）1件の確認、(c) **26.2・オーバーワールドでの setTime 受理確認**（設計書 §3.2 で対応範囲をオーバーワールド実行に限定したため、26.2 実機での受理は未確認のまま）。非オーバーワールド実行は設計上の対象外。
- 次フェーズへの申し送り: 実ゲーム受入は Phase 4 と同様に代表 ZIP を決定論生成して `saves\<ワールド名>\datapacks` へ導入して確認する。特に 26.2 の setTime は clock 省略時の default clock 依存があるため、乖離が出たら明示 clock 化（`time of minecraft:overworld set <value>`）を別スコープで起票する。
- 関連ドキュメント: [修正設計書_Java時刻天候アクション設定化_2026-07-24.md](修正設計書_Java時刻天候アクション設定化_2026-07-24.md) / [レビュー_Java時刻天候アクション設定化設計_2026-07-24.md](レビュー_Java時刻天候アクション設定化設計_2026-07-24.md)
- ステータス更新: Phase 5 を完了フェーズへ移動。apps/web v0.7.3 を実装済み・本番反映済み・実ゲーム受入待ちとした。

### Phase 4: Java多パターン現行再評価対応 v0.7.2 — 完了

- 完了日: 2026-07-23
- 実装した内容: 2026-07-22 現行再評価レビュー（No-Go、P1 2件・P2 5件・P3 1件）の全8件を解消。REV-P1-01: 未知判別値（kind/recipeType/resourcepack.pattern）を fail-close する二段防御（`validateJava` の既知enum検証＋early return、`capabilitiesForSpec`/`generateJavaFiles` の catch-all 排除＋未知値 throw）。REV-P1-02: `script.trigger.interval`/`script.action.message` を phase 0 へ移し phase 0 を v0.2.0 ベースライン（shaped/lang/interval×message）に一致。`RELEASED_JAVA_PHASE=5` は据え置き、未受入公開は明示 waiver として記録。REV-P2-01: Java検証で `rule` を必須 context 化（欠落は「設定未取得」で拒否）、`page.tsx` の config-ready 条件に `javaVersionRule` 追加。REV-P2-02: setTime/setWeather を trigger=interval 専用へ制限（event 系は検証エラー）。REV-P2-03: 生成 README の導入先を pack 種別ごとの確定文へ（datapack=`saves\<ワールド名>\datapacks`＋`/datapack list enabled`、resourcepack=`resourcepacks`＋ゲーム内設定）。REV-P2-04: 版×capability マトリクスへ期待ファイル path 集合一致を追加。REV-P2-05: 機能設計書へ supersede 注記、README 正本案内に後続修正設計書・現行契約を併記。REV-P3-01: 機能設計書ヘッダ/変更履歴/§13・設計書インデックス・実装ログを現行へ同期。apps/web を v0.7.2 へ更新。
- 反映コミット: `5f80476`（実装本体：src/tests/version）／ドキュメント同期は後続の docs コミット
- 検証結果: `npm run lint`（tsc）pass / Vitest 8ファイル45件 pass（既存38件＋新規7件：未知判別値の validate/build/generator 拒否・phase 0 ベースライン・rule 欠落 cooking 拒否・setTime の event 拒否/interval 許可・README 導入先の種別別・版×capability 期待 path 集合）/ `npm run build` pass（既存 NFT trace 警告1件は継続）/ WPF Release build pass（警告0・エラー0）。**実ゲーム受入：未実施（waiver）**。
- 残課題・既知の問題: **P1-02 waiver は解消済み**。2026-07-24、Phase 1〜5 代表7ケース（夜間暗視 / 精錬 / 鍛冶 / 食事イベント / loot差し替え / 時刻設定 / itemModelSwap）を Java **1.21.5** で実ゲーム受入し、全て正常動作を確認（決定論生成した代表 ZIP を `saves\<ワールド名>\datapacks` および `resourcepacks` へ配置）。残る受入未実施は他対象版（1.21 / 1.21.4 / 1.21.7 / 26.2）と、setTime/setWeather の 26.2 World Clock 構文（単純出力・overworld 想定、機能設計書§13 ゲート#5）。
- 次フェーズへの申し送り: 1.21.5 の代表受入は完了。setTime/setWeather は v0.7.3（[修正設計書_Java時刻天候アクション設定化_2026-07-24.md](修正設計書_Java時刻天候アクション設定化_2026-07-24.md)）で「設定」/one-shot・event 許可へ改める。他版の実ゲーム受入と 26.2 setTime 受理確認は別スコープで順次実施し、不合格時は該当修正設計書を新規起票する。
- 関連ドキュメント: [修正設計書_Java多パターン現行再評価対応_2026-07-23.md](修正設計書_Java多パターン現行再評価対応_2026-07-23.md) / [レビュー_Java多パターン対応設計_現行再評価_2026-07-22.md](レビュー_Java多パターン対応設計_現行再評価_2026-07-22.md) / [機能設計書_Java多パターン対応_2026-07-19.md](機能設計書_Java多パターン対応_2026-07-19.md)
- ステータス更新: Phase 4 を完了フェーズへ移動。apps/web v0.7.2 を実装済みとした。2026-07-24、Phase 1〜5 代表7ケースの Java 1.21.5 実ゲーム受入合格により P1-02 waiver を解消し、当フェーズを「実ゲーム受入完了（1.21.5）」へ更新。

### Phase 3: Java多パターン実装精査対応 v0.7.1 — 完了

- 完了日: 2026-07-20
- 実装した内容: M1: `validateJavaRecipe`のshaped分岐へ「keyの記号⊆patternの記号」の逆方向検証を追加（未使用key記号を持つ無効ZIP生成を防止）。M2: capability判定を`spec.ts`の`javaCapabilitiesForValidation`から`pattern-catalog.ts`の`capabilitiesForSpec`へ一本化。M3: `fetchOpenAiResponse`の`fetch`にモデル候補ごと`signal: AbortSignal.timeout(120_000)`を付与。M4: `JAVA_GENERATOR_HANDLERS`のno-op式文を削除し`JAVA_GENERATOR_CAPABILITY_COVERAGE`へ改名（役割をID登録漏れ検知に限定）。M5: script系データパックのREADME.txtにnamespace衝突注意を追記。apps/webをv0.7.1へ更新（README.txtの版表記も追随）。
- 反映コミット: `e013772`（実装本体）
- 検証結果: `npm run lint` pass / Vitest 8ファイル38件 pass（既存31件+M1再現テスト・buildPack境界テスト・README注記テスト・timeout/fallbackテスト計7件を追加）/ `npm run build` pass（既存のNext.js NFT trace警告1件は継続）/ WPF Release build pass（警告0・エラー0）。
- 残課題・既知の問題: なし。M3のタイムアウト値120秒の妥当性は実績ベースで別途検討（環境変数化は今回YAGNIで見送り、設計書§10参照）。v0.7.0のPhase 1〜5代表ケースの実ゲーム受入はPhase 2からの継続課題として別スコープのまま。
- 次フェーズへの申し送り: 本バッチはJava多パターン対応系統の最終修正であり、以降の追加修正バッチは予定しない。次に着手する場合は新規スコープとして設計書から起票する。
- 関連ドキュメント: [修正設計書_Java多パターン実装精査対応_2026-07-20.md](修正設計書_Java多パターン実装精査対応_2026-07-20.md) / [レビュー_Java多パターン実装精査対応設計_2026-07-20.md](レビュー_Java多パターン実装精査対応設計_2026-07-20.md)
- ステータス更新: Phase 3を完了フェーズへ移動。apps/web v0.7.1を実装済みとした。

### Phase 2: Java多パターン対応 v0.3.0〜v0.7.0 — 完了

- 完了日: 2026-07-19
- 実装した内容: capabilityカタログを単一の可用性・文言正本として追加し、Bedrock/Javaのspecとchat schemaを分離。Java build gate、`javaScript`のinterval/イベント6トリガー・5条件・7アクション、レシピ8種、ブロックloot差し替え、lang/itemModelSwap、26.2 min/max pack metadataとtime_check clock、決定論description、API/UI連動を実装。apps/webをv0.7.0へ更新。
- 反映コミット: `6871559`（実装本体）
- 検証結果: `npm run lint` pass / Vitest 7ファイル31件 pass（5版×全capabilityマトリクス、Bedrock Codex JSON境界、client/server gate、生成ZIPを含む）/ `npm run build` pass / WPF Release build pass（警告0・エラー0）。Next.js既存NFT trace警告1件は継続。2026-07-20、Java 1.21.5で既存3種（recipe / 5秒通知 / lang）の実ゲーム受入に合格。データパック2種は対象ワールドの `saves/New World/datapacks` へ配置することで正常動作を確認。
- 残課題・既知の問題: Phase 1〜5の新代表ケース（夜間暗視、精錬・鍛冶、食事イベント、loot・時刻固定、itemModelSwap）は未実施。親 `PROJECT_OVERVIEW.md` とObsidianは現在の書き込み可能範囲外のため未更新。
- 次フェーズへの申し送り: 実ゲーム受入は各代表ZIPを対象ワールドへ導入し、`/datapack list`、再発火・非遅延発火、見た目、シルクタッチ注意事項を確認する。不合格時は該当Phaseの修正設計書を新規作成する。
- 関連ドキュメント: [機能設計書_Java多パターン対応_2026-07-19.md](機能設計書_Java多パターン対応_2026-07-19.md) / [レビュー_Java多パターン対応設計_2026-07-19.md](レビュー_Java多パターン対応設計_2026-07-19.md)
- ステータス更新: Phase 2を完了フェーズへ移動。apps/web v0.7.0を実装済み・実ゲーム受入待ちとした。

### Phase 1: Java版対応 v0.2.0 — 完了

- 完了日: 2026-07-19
- 実装した内容: Bedrock既存機能を維持し、Java 1.21系向けのレシピ・定期チャット通知・langリソースパックを決定論的に生成。版切替UI、対象版厳密化、Java専用出力、editionロック、遅延応答破棄を実装。実装レビューP2-01としてrecipe pattern/keyの行長・同幅・記号定義・key記号長検証を追加し、不正時はZIP生成前に拒否。
- 反映コミット: `786e280`、`26758fb`、`e1c0f5f`、`44bc3e3`（masterへfast-forward）
- 検証結果: `npm run lint` pass / Vitest 6ファイル31件 pass / `npm run build` pass / WPF Release build pass（警告0・エラー0）/ self-contained publish pass。本番ショートカット起動でEXE SHA-256 `9D3F815533BAFB5C0C11969113165BACF221062441A3B6998AE5B9F5F5614B96`、Next Build ID `RQLy13R1Y4oxAfVzsj5fz`、WPFウィンドウ、Node子プロセス、ポート3031、HTTP 200、Java版UI、`JAVA_TARGET_VERSION=1.21.5`、fresh logを確認。2026-07-20実ゲーム受入結果: recipe 合格 / 5秒通知と`/reload`後の非二重化 合格 / lang 合格。データパックは `C:\Users\souta\AppData\Roaming\.minecraft\saves\New World\datapacks` へ配置。
- 残課題・既知の問題: なし（必須受入3種を完了）。Next.js本番ビルドの既存NFTトレース警告1件、およびレビューP3任意3件は継続。
- 次フェーズへの申し送り: v0.2.0の必須受入3種は完了。データパックはグローバルフォルダではなく、対象ワールドの `saves/<ワールド名>/datapacks` へZIPのまま配置する。今後はv0.7.0の各代表ケースを順次受入する。
- 関連ドキュメント: [機能設計書_Java版対応_2026-07-18.md](機能設計書_Java版対応_2026-07-18.md) / [レビュー_Java版対応設計_2026-07-18.md](レビュー_Java版対応設計_2026-07-18.md) / [レビュー_Java版対応実装_2026-07-19.md](レビュー_Java版対応実装_2026-07-19.md)
- ステータス更新: Phase 1を完了フェーズへ移動。apps/web v0.2.0をmaster・本番publishへ反映済み。
